[user]
	email = mail@lauritz.me
	name = Lauritz Hilsøe
[credential]
	helper = osxkeychain
[alias]
	db  = "!f() { git push origin :refs/heads/$1 && git branch -D $1; }; f"
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%C(bold blue) <%an>%Creset' --abbrev-commit
	reb = "!f() { git rebase HEAD~$1 --interactive; }; f"
[color]
	ui = true
[core]
	editor = nvim
	excludesfile = ~/.gitignore
[apply]
	whitespace = nowarn
[push]
	default = simple
[branch]
	autosetuprebase = always
[pull]
	rebase = true
[hub]
	host = github.itu.dk
[rebase]
	autosquash = true
